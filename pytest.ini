[pytest]
pythonpath =
    .
testpaths =
    tests
python_files =
    test_*.py
addopts =
    -ra
markers =
    validation: repo validation tests that do not require optional data-engineering packages
    compatibility: package compatibility tests that may require airflow, duckdb, polars, sqlalchemy, or other optional dependencies
